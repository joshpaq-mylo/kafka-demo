# Logs
logs
*.log

# Runtime data
pids
*.pid
*.seed

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directory
# https://www.npmjs.org/doc/misc/npm-faq.html#should-i-check-my-node_modules-folder-into-git-
node_modules

# Debug log from npm
npm-debug.log

# Dev environment
.idea
.vscode

devSecrets.json

# if nsp fails and you still need to build:
# 1. in .travis.yml add environment variable NSP_EXCEPTIONS=1,2,3 where 1,2,3 correspond to the advisory numbers to ignore
# 2. commit changes via: git commit -n -a -m "commit message"
# - this seems to be a better alternative than checking in .nsprc and removing the proxy
.nsprc

*.map

build/
config/data/
